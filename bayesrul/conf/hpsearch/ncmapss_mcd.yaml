# @package _global_

defaults:
  - /hpsearch/study: study
  - /hpsearch/objective: mcd
  - override /experiment: ncmapss_mcd

hpsearch:
  monitor: nll/val
  study:
    study_name: ncmapss_mcd

trainer:
  max_epochs: 20

callbacks:
  early_stopping: null
