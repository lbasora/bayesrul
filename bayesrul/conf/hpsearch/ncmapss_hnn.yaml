# @package _global_

defaults:
  - /hpsearch/study: study
  - /hpsearch/objective: hnn
  - override /experiment: ncmapss_hnn

hpsearch:
  monitor: nll/val
  study:
    study_name: ncmapss_hnn

trainer:
  max_epochs: 50

callbacks:
  early_stopping: null
