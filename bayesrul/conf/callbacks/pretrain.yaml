defaults:
  - model_checkpoint
  - rich_progress_bar
  - _self_

ckpt_path: null

model_checkpoint:
  filename: "pretrained"
  monitor: "mse/val"
  mode: "min"
  save_last: False
  auto_insert_metric_name: False
